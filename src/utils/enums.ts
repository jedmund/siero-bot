export enum DrawableItemType {
  WEAPON = 0,
  SUMMON = 1,
}

export enum GachaBucket {
  WEAPON = 1,
  SUMMON = 2,
  LIMITED = 3,
  RATEUP = 4,
}

export enum Promotion {
  PREMIUM = "premium",
  CLASSIC = "classic",
  LEGEND = "legend",
  FLASH = "flash",
}

export enum Rarity {
  R = 1,
  SR = 2,
  SSR = 3,
}

export enum Element {
  NULL = 0,
  WIND = 1,
  FIRE = 2,
  WATER = 3,
  EARTH = 4,
  DARK = 5,
  LIGHT = 6,
}

export enum Season {
  VALENTINES = "valentines",
  SUMMER = "summer",
  HALLOWEEN = "halloween",
  HOLIDAY = "holiday",
}
